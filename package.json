{
  "name": "pokemon-card-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pokemon TCG Collector Simulator - a collection-management and card-market simulator with pack opening as the emotional centerpiece.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace=@pcs/web",
    "build": "npm run build --workspace=@pcs/web",
    "start": "npm run start --workspace=@pcs/web",
    "lint": "npm run lint --workspace=@pcs/web",
    "typecheck": "tsc --build --verbose",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "drizzle-kit generate --config=packages/db/drizzle.config.ts",
    "db:push": "tsx scripts/db/push.ts",
    "db:reset": "tsx scripts/db/reset.ts",
    "data:sets": "tsx scripts/import/import-sets.ts",
    "data:cards": "tsx scripts/import/import-cards.ts",
    "data:prices": "npm run data:tcgcsv && npm run data:fetch-prices && npm run data:apply-prices",
    "data:all": "npm run data:sets && npm run data:cards && npm run data:prices && npm run data:price-packs && npm run data:pack-prices",
    "data:validate": "tsx scripts/validate/validate-data.ts",
    "simulate": "tsx scripts/simulate/simulate-packs.ts",
    "data:fetch-prices": "tsx scripts/import/fetch-prices.ts",
    "data:apply-prices": "tsx scripts/import/apply-prices.ts",
    "data:apply-prices-supabase-2026": "tsx scripts/import/apply-prices-supabase-2026.ts",
    "data:price-packs": "tsx scripts/simulate/price-packs.ts",
    "db:rebuild": "npm run db:reset && npm run db:push && npm run data:sets && npm run data:cards && npm run data:apply-prices && npm run data:price-packs && npm run data:pack-prices",
    "data:tcgcsv": "tsx scripts/import/fetch-tcgcsv.ts",
    "data:pack-prices": "tsx scripts/import/apply-pack-prices.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "drizzle-kit": "^0.31.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.2": true,
    "esbuild@0.18.20": true,
    "unrs-resolver@1.12.2": true
  }
}
